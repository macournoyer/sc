#!/usr/bin/env ruby

name = ARGV.first || raise("usage: sc NAME")

FileUtils.mkdir_p name
FileUtils.cp_r File.dirname(__FILE__) + "/../template/*", name

puts <<-USAGE
## Now what? ##
cd #{name}
rackup # to start dev server
rack   # to compile
USAGE